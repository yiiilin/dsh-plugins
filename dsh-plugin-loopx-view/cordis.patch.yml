# dsh-plugin-loopx-view bundle patch.
#
# The Host half reads the exact current DSH Session and exposes a read-only
# projection route. The Client half contributes the session-scoped
# conversation.view tab.

- insert:
    - id: loopx-view
      name: '@yiln-dsh/dsh-plugin-loopx-view'
      inject:
        - agents
        - connection
      config: {}
