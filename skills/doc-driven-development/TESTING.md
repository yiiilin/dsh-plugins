# v0.2.3 验证记录

验证日期：2026-09-20。环境：Linux、Node.js v22.16.0、可用本地 Git。运行只使用 Node.js 标准库；没有联网、安装第三方依赖或调用任何模型。

## 1. 包检查与回归结果

执行 `node scripts/check.mjs`：包清单哈希、版本、必需文件、相对链接、全部 JavaScript 语法均通过。

执行的自动测试共 **292 项，292 通过，0 失败、0 跳过**：原有 227 项回归全部保留；本版增加 65 项共同设计规则/正文检查/集成测试。测试均在隔离目录进行，不执行用户项目代码。

```sh
# 完整检查，包括所有隔离测试。
node scripts/check.mjs

# 只重跑新增结构与集成测试。
node --test tests/design.mjs
```

原有测试继续覆盖安全安装、init 完整转发、半安装修复、逐字节规范保护、冲突停止、回滚、索引、领域布局、归属、证据、代码快照、渐进/全量进度和交付门禁。

新增测试见 [tests/design.mjs](tests/design.mjs)，覆盖：

- 中文、英文和自定义语言标题/标记；三层顺序、重复、缺失、空正文、空验收和简单占位；代码围栏和普通 HTML 注释不能伪造真实结构。
- ASCII 处理/数据/状态图的标记与章节位置；闭合围栏、tildes、CRLF、中文节点、方向连接符、不适用原因、pending 和共享图引用；缺失/历史/非受管/外部/越界及循环引用处理。
- observed/proposed 保留警告；accepted 或显式 --design 发现结构缺口；旧文档仅报告，--base 选择新建/实质变化，防止删除格式字段规避；metadata/evidence 更新与无关历史文档的兼容。
- “合法元数据 + Notes，没有需求/概要/详细”的原问题：新格式获批或严格定稿检查会失败；旧未迁移文档明确报告 design incomplete，不再把结构结果当作正文设计完成。
- shipped 模板保留真实待填项，不伪造完成；完整假设示例的结构可通过，但仍为 proposed/not-run，未决技术问题不因此获得实现授权。
- 项目受管区块和新会话探测包含主动调查/讨论/质疑与三类图规则；首次安装仍保护原规范、配置、正文，重复安装零写入。

## 2. 真实 v0.2.2 发行包升级验证

额外使用本次会话已有的真实 v0.2.2 TGZ，在隔离项目中运行其原安装器，再由 v0.2.3 升级，完成 **11 项端到端断言，全部通过**。这组发布验证独立于上述 292 项包内测试，不冒充真实业务仓库或真实 agent 运行。

验证了预览零写入；升级计划不包含已有配置/设计正文；包与收据升级到 0.2.3；AGENTS/CLAUDE 受管区块之外的原始字节完全一致；UTF-8 BOM、中文、混合换行、无末尾换行及安装后新增个人规范保留；索引、配置、源码和手写设计保持不变；重复安装零修改；卸载保留个人内容；未纳管但未修改的 v0.2.2 包也可安全识别升级；本地改动受管区块后拒绝操作且零写入。

## 3. 发行归档验证

TGZ 以 `doc-driven-development/` 为单一根目录，不包含测试工作树、临时产物或用户文件。解包后再次运行同一完整自检，292 项再次通过；归档内容与发行目录逐文件 SHA-256 一致。压缩包旁提供 SHA256SUMS 文本。

## 4. 不能据此声称的事情

没有在用户实际业务仓库执行全量接管，没有在真实多模型/多宿主中验证本版共同设计行为，没有在 Windows/macOS 或其他 Node.js 版本实测。安装适配延续已有实现，不因本次改动声称外部宿主加载规则已重新在线核实。

静态规则测试只能确认关键规则文本存在，不能证明模型真的执行了它。正文检查不能证明图正确、分支完整、代码符合文档、人的批准真实或测试命令实际运行。使用 [examples/scenarios.md](examples/scenarios.md) 中的具体任务在真实 agent 上试点；不要只让模型口头承诺遵守。

`doctor` 的 runtime 仍为 not-run，`stats.design.semantic` 仍为 not-assessed。本版不增加隐藏的强制权限或审批平台，不将 CI 通过解释为“AI 从未提前写过代码”。
