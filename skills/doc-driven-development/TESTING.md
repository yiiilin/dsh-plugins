# v0.2.4 验证记录

验证日期：2026-09-21。环境：Linux、Node.js v22.16.0、Git 2.47.3。仅使用 Node.js 标准库和本地 Git；未联网、未安装第三方运行依赖、未调用模型或外部 agent。

## 1. 包自检与自动回归

`node scripts/check.mjs` 执行发行清单哈希/版本、必需文件、Markdown 相对链接、JavaScript 语法与隔离测试。

实际结果：**45 个发行文件、97 个本地链接；376 项测试通过，0 失败，0 跳过。** 原有 292 项回归保留（旧版正向验证夹具补了真实格式的规格绑定，非真实批准）；新增 84 项机械/集成测试。所有测试使用临时项目，真实用户仓库不被修改。

新增测试覆盖：

- 规格身份、修订/正文/图/验收/归属/依赖变化；E 子树排除避免自失效，围栏中的伪标题不能隐藏规范；嵌入 E 的 R/C 定义被拒绝；字段重复与占位完成记录拒绝。
- 相加改相减而源码/旧证据不变的反例；忘记增加修订；新旧证据共存、旧失败不干扰新版本；未绑定旧记录普通警告但不能严格交付；跨文档 Covers 需要各自目标绑定；新执行记录的 Environment。
- 真正的设计评审记录与执行验证分开；评审尚未执行时允许 unknown，不迫使伪造审阅者/结论；阻塞项、规格变化、代码变化会使评审门禁不能通过。无 review 标志不冒充实际评审。
- 默认只读上下文、Doc-ID/条目/路径、中文短词字面检索、显式依赖和反向影响、普通链接不变成强依赖、未知/重复身份、历史隔离、删除关系影响、有限循环遍历、截断和参数错误。
- 当前文件/代码/配置/项目规则变化、同提交分支切换、不同工作树隔离；保存快照不使其自身过时；只允许创建新的专用快照，不覆盖 AGENTS、配置、原设计、任意 JSON、个人文件或符号链接目标。
- 入口和参考规则具有即时记录、场景解释、评审核实、版本刷新和双向核对的文本约定。**这部分仅检验规则存在，不证明模型会执行。**

重现：

```sh
node scripts/check.mjs
node --test tests/closure.mjs tests/context.mjs
```

## 2. 真实 v0.2.3 包升级与实际程序反例

额外使用本次会话中真实 v0.2.3 TGZ，在隔离项目执行原安装器，再执行 v0.2.4 安全升级与检查；**15 项端到端断言全部通过**，独立于上方 376 项包内测试。

验证了预览零写入；有收据和无收据原发行包升级；doctor activation ready 但 runtime not-run；原 AGENTS/CLAUDE 区块之外保留，UTF-8 BOM、中文、CRLF、无末尾换行以及安装后新增规范不受损；原 JSON 配置、导航、设计正文保留；重复安装零修改；卸载保留个人 skill 文件、上下文快照、配置及原规范；本地修改过的区块拒绝升级而不覆盖。

程序反例实际执行一个隔离的有限数值相加模块及其 Node 测试（正常和非法输入，共 2 项）。把文档要求改成相减、递增修订但保留旧代码与证据：旧测试仍通过，v0.2.3 的 `--base --design --release` 仍通过，v0.2.4 拒绝未绑定新契约的旧证据。随后实际修改实现和验收断言、重新执行 2 项测试并记录本次规格/代码/环境，新严格交付检查通过。

审批字段始终是明确标注的隔离测试夹具，不是人的实际批准。程序/测试运行是真实执行，不能据此声称任何真实业务系统经过验证。

## 3. 发行归档

TGZ 单一根目录为 `doc-driven-development/`，只含清单内发行文件与清单本身，不含试验仓库、其他版本归档、会话数据或临时产物。归档解包后再次执行完整自检，376 项通过；内容逐文件 SHA-256 与发行目录一致。压缩包的 SHA-256 单独提供。

## 4. 未验证范围与机制边界

未在真实用户业务仓库接管、未做真实多模型/多宿主长对话评估、未实测 Windows/macOS/其他 Node 版本、未测大型仓库查询性能。现场 agent 评估计划见 [examples/evaluation.md](examples/evaluation.md)，**尚未执行**；不要用静态文本检查冒充模型行为测试。

证据检查不证明人的批准真实、测试一定执行、场景完备或代码语义符合文档。hash 不是防篡改审批，不允许把新身份贴回旧通过结果。设计评审记录不等于独立审查，独立工具不可用时如实自审。未绑定/过时证据保留限制，不能视为当前验证。

context 是按当前扫描范围重建的非原子文件快照，不是持续监控或阅读证明；依赖关系只来自显式声明，不能保证语义影响全覆盖；排除、大文件、符号链接及潜在密钥过滤边界沿用 inventory，秘密也可能出现在未过滤的源码/标题里，仍需使用者和 AI 控制写入。未使用 SQLite、向量服务或额外运行依赖。

安装器继续只更新自身拥有且未被改动的块和文件；不能保证所有宿主自动加载，也没有新增工具权限钩子或 CI 防篡改平台。doctor runtime、context reading/semantic、设计检查 semantic 不会因为包测试通过而自动变成已验证。
