# 本版验证记录

版本：0.2.0。此记录仅说明技能包本身的检查，不代表用户项目已被分析，也不代表不同 AI 模型都经过实测。

## 执行环境

本次构建环境：Linux、Node.js v22.16.0。运行时只使用 Node.js 标准库；涉及变更基线的测试使用本地 Git，不联网，不安装依赖。

## 检查范围

执行包元数据、目录/相对链接与 JavaScript 语法检查；使用临时仓库执行正常与反例测试，覆盖安全初始化、索引保留、状态/确认、归属、证据、代码优先的结构边界、渐进与全量进度、哈希失效、Git 差异和严格交付条件。

测试文件：[tests/run.mjs](tests/run.mjs)。部分测试专门构造假审批/假证据字段，以确认结构检查边界；这些字段不是实际项目的审批或执行结果。另有一个隔离的可运行加法模块及其真实测试，验证工具链无需第三方依赖即可走通。

## 重现

```sh
node scripts/check.mjs
node --test tests/run.mjs
```

## 实际结果（2026-09-18）

- `node scripts/check.mjs`：包检查通过；21 个发行文件，38 个相对链接有效，全部 JavaScript 语法检查通过。
- `node --test tests/run.mjs`：76 个测试通过，0 个失败，0 个跳过。
- 自动测试中实际运行了一个隔离示例程序的测试；其他项目级证据与审批字段为测试夹具，专门验证结构规则，不冒充真实业务运行记录。

发行包已在临时目录解包并重复执行同一自检，76 项测试再次全部通过；ZIP/TGZ 的文件内容一致性已通过逐文件 SHA-256 比较核对。具体归档哈希在压缩包旁的 SHA256SUMS 文本中。


## 尚未验证

没有使用用户的实际业务仓库执行全量接管；没有进行多模型、多宿主长期试点；没有在 Windows/macOS 或其他 Node.js 版本上实测。检查不证明自动理解、真实批准、实际测试执行真实性、性能或业务正确性。

默认扫描排除项、大文件、符号链接、子模块、没有运行环境时的降级等限制，见 [README.md](README.md) 和 [ADOPTION.md](ADOPTION.md)。人工/模型行为场景见 [examples/scenarios.md](examples/scenarios.md)。
