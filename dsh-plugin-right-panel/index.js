/**
 * @yiln-dsh/dsh-plugin-right-panel — Client-only host package.
 *
 * The browser half owns the right-side `details` Slot. The Host row is
 * intentionally inert so the bundle can be installed and composed uniformly
 * with the other DSH plugins.
 */
export const name = 'right-panel'
export const inject = []

export function apply() {}
